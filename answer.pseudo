#include<iostream>
#include<string.h>
#include<stdlib.h>
#include<time.h>

using namespace std;

void test(string a)
{
    srand (time(NULL));
    int b = rand()%16;
    if(b<6)
        b+=6;
    cout<<a<<b;
}

int main()
{
    test("Aravind");
    return 0;
}
